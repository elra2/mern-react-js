import React, {Component} from 'react';
import ThingsToDo from './ThingsToDo';

class TodoList extends Component {
  render (props) {
    return (
        <div>
            {props.todolist.map((todolist, id)
            
            
            
            )}
        </div>
    );
  }
}

export default TodoList;